import 'package:flutter/material.dart';

// TODO modify padding as needed
/// Horizontal and vertical padding at the borders of each screen
const hPadding = 18.0;
const vPadding = 20.0;

const horizontalEdgeInsets = EdgeInsets.symmetric(horizontal: hPadding);
const verticalEdgeInsets = EdgeInsets.symmetric(horizontal: vPadding);
const wrapEdgeInsets = EdgeInsets.symmetric(
  horizontal: hPadding,
  vertical: vPadding,
);
const defaultRadius = 6.0;
